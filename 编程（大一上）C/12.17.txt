/*Description
键盘输入6个整数存到二维数组a[2][3]中，要输出它们，请填空：



int main()



{



    int a[2][3], i, j;



    _____________;    //定义一个指向变量的指针变量p1并赋初值



    _____________;    //定义一个指向一维数组的指针变量p2并赋初值



    for(i=0;i<=1;i++)



         for(j=0;j<=2;j++)



                      scanf("%d", &a[i][j]);



   for(i=0;i<=1;i++)



        for(j=0;j<=2;j++)



               _____________________;    //利用p1输出每个整数



    printf("\n"); 



   for(i=0;i<=1;i++)



        for(j=0;j<=2;j++)



               _____________________; //利用p2输出每个整数



   printf("\n"); 



   for(i=0;i<=1;i++)



       for(j=0;j<=2;j++)



               _____________________; //利用数组名a输出每个整数，用指针法，不要用下标法



    printf("\n"); 



    return 0;



}


Input
输入6个整数


Output
按要求输出数据，每个数据占3位


Sample Input
2 3 5 1 6 4
Sample Output
  2  3  5  1  6  4
  2  3  5  1  6  4
  2  3  5  1  6  4*/
#include<stdio.h>
int main()
{
    int a[2][3], i, j;
    int *p1=a[0];    
    int (*p2)[3]=a;   
    for(i=0;i<=1;i++)
         for(j=0;j<=2;j++)
                      scanf("%d", &a[i][j]);
   for(i=0;i<=1;i++)
        for(j=0;j<=2;j++)
               printf("%3d",*(p1+i*3+j));
    printf("\n"); 
   for(i=0;i<=1;i++)
        for(j=0;j<=2;j++)
               printf("%3d",p2[i][j]);
   printf("\n"); 
   for(i=0;i<=1;i++)
       for(j=0;j<=2;j++)
		   printf("%3d",*(*(p2+i)+j));
    printf("\n"); 
    return 0;
}
----------------------------------------------------------------------------------------------------------------------------------------
/*Description
程序有框架如下。程序的功能是：若键盘输入的字符串比p对应的串大，则输出键盘输入的串，
否则输出p对应的串，程序缺两处代码，请你把所缺代码提交。注意：不允许使用strlen/strcmp/strcpy/strcat等函数

************************************

  //缺头文件和函数定义

************************************
int main()   
{   
    char s[21],*p="hello";
    gets(s);
    sub(__________________);  //缺少参数 
    printf("%s\n",s); 
    return 0;   
}
Input
输入一个不超过20字符的字符串
Output
输出大串
Sample Input
abc
Sample Output
hello*/
#include<stdio.h>
#include<string.h>
void sub(char *p,char *s)
{
	int i,k;
	for(i=0;i<5;i++)
	{
		if(p[i]>s[i])
		{
			k=1;
			break;
		}
		else
		{
			k=0;
			break;
		}

	}
	if(i<5)
		if(k=1)
		{
			for(i=0;p[i]!='\0';i++)
				s[i]=p[i];
			s[i]='\0';
		}
}
int main()   
{   
    char s[21],*p="hello";
    gets(s);
    sub(p,s);  //缺少参数 
    printf("%s\n",s); 
    return 0;   
}
-------------------------------------------------*上错
#include<stdio.h>
#include<string.h>
void sub(char *s,char *p)
{
	int i=0;
	while(*(p+i)!=0&&*(s+i)!=0)
	{
		if(*(p+i)!=*(s+i))
			break;
		i++;
	}
	if(*(p+i)>*(s+i))
	{
		for(i=0;*(p+i)!=0;i++)
			*(s+i)=*(p+i);
		*(s+i)=0;
	}
}
int main()
{   
    char s[21],*p="hello";
    gets(s);
    sub(s,p);  //缺少参数 
    printf("%s\n",s); 
    return 0;   
}
----------------------------------------------------------------------------------------------------------------------------------------
/*第1题.12.利用指针变量访问变量：被调函数修改主调函数的值
Description
主函数给int a[3][5]输入数据,被调函数找出最大数和最小数，存入主函数定义的变量中，主函数输出。部分程序如下所示。
#include <stdio.h>
   数已在后台以正确声明   
                                    \\函数定义
int main()
{
         int a[3][5],max,min,i,j;
          for(i=0;i<=2;i++)
                for(j=0;j<=4;j++) 
                  scanf(“%d”,&a[i][j]);
                                           \\函数调用
          printf(“%d,%d\n”,max,min);
          return 0;
}
Input
输入15个数据
Output
最大数,最小数
Sample Input
Sample Output
Code 1 here:
Code 2 here:
*/
#include <stdio.h>
void f(int *p,int *max,int *min)
{
	int i;
	*max=*min=*p;
	for(i=1;i<15;i++)
	{
		if(p[i]>*max)
		{
			*max=p[i];
		}
		if(p[i]<*min)
		{
			*min=p[i];
		}
	}
}
int main()
{
	     int a[3][5],max,min,i,j;
		  for(i=0;i<=2;i++)
		        for(j=0;j<=4;j++) 
		          scanf("%d",&a[i][j]);

f(&a[0][0],&max,&min);
	  printf("%d,%d\n",max,min);
		  return 0;
}
----------------------------------------------------------------------------------------------------------------------------------------
/*第2题.12.利用指针变量访问变量：编写被调函数使数组中每个元素都变为原来的两倍
Description
主函数定义数组int a[5],由键盘输入数据,编写被调函数使数组中每个元素都变为原来的两倍.



#include <stdio.h>

   函数已在后台以正确声明   



                                    \\函数定义

int main()

{

         int a[5],i;



         for(i=0;i<=4;i++)

                  scanf("%d\n",&a[i]);

                                           \\函数调用

          for(i=0;i<=4;i++)

                  printf("%d\n",a[i]);

          return 0;

}


Input
Output
Sample Input
Sample Output
Code 1 here:
Code 2 here:
*/
#include <stdio.h>
void f(int *p)
{
	int i;
	for(i=0;i<=4;i++)
	{
		p[i]=p[i]*2;
	}
}
int main()
{
	     int a[5],i;
		 for(i=0;i<=4;i++)
			 scanf("%d",&a[i]);

f(&a[0]); 
 for(i=0;i<=4;i++)
	printf("%3d",a[i]);
 return 0;
}

----------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------